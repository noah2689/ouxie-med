# CLAUDE.md

## 项目名称

瓯械健康

## 项目类型

家庭健康管理微信小程序前端 Demo

## 项目目标

本项目用于 UI/UX 作品集展示，目标是开发一个可交互的微信小程序前端 Demo，用于展示家庭健康指标记录、家庭成员切换、指标详情查看、设备管理、提醒设置和 AI 辅助解读等核心流程。

当前阶段只做前端 Demo：

- 不接真实后端
- 不做登录系统
- 不接真实设备
- 不调用真实 AI
- 不做真实数据同步
- 所有数据使用本地 mock 数据
- 重点是还原高保真设计和交互流程

项目不是医疗问诊工具，不提供诊断、治疗、处方或用药建议。

---

## 技术栈

使用微信小程序原生开发：

- WXML
- WXSS
- JavaScript
- JSON
- 本地 mock 数据

禁止使用：

- uni-app
- Taro
- React
- Vue
- 第三方 UI 组件库
- TDesign 整套接入
- 暗黑模式
- 外部图表库
- 后端接口
- 云开发
- 登录授权

如确实需要 Toast、Picker、Popup 等基础交互，可以参考组件库的交互思路，但不得引入整套 UI 框架，也不得破坏当前高保真视觉风格。

---

## 核心开发原则

### 1. 高保真优先

本项目首先是作品集 Demo，不是业务上线项目。

开发优先级：

1. 视觉还原
2. 核心交互闭环
3. 页面完整度
4. 数据结构清晰
5. 代码可维护

不要为了快速完成功能而牺牲视觉还原。

### 2. 分阶段开发

不要一次性开发所有页面。

每次只做一个阶段，完成后停止，等待人工验收。

开发时必须先说明：

- 本阶段目标
- 将修改哪些文件
- 为什么修改这些文件
- 本阶段不做什么

完成后必须说明：

- 修改了哪些文件
- 完成了哪些功能
- 哪些只是 mock 或占位
- 下一阶段建议做什么

### 3. 不允许自行扩展需求

不得自行新增以下内容：

- 登录注册
- 用户权限
- 后端 API
- 真实设备绑定
- 真实 AI 对话
- 会员体系
- 商城
- 资讯流
- 健康报告下载
- 复杂图表库
- 暗黑模式
- 医疗问答功能

如果发现需要扩展，先停止并询问。

---

## 产品定位

瓯械健康是一个面向家庭场景的健康指标记录与管理小程序。

核心能力：

- 查看家庭成员健康概览
- 记录血压、血糖、尿酸
- 查看指标详情和历史记录
- 切换家庭成员
- 管理设备状态
- 设置记录提醒
- 使用 AI 助手辅助理解近期记录变化

产品关键词：

- 家庭健康管理
- 指标记录
- 多成员数据
- 趋势查看
- 设备管理
- 提醒设置
- AI 辅助解读
- 轻医疗
- 克制表达
- 数据记录工具

---

## 设计风格

整体风格：

- 清爽
- 专业
- 轻医疗
- 家庭健康管理
- 数据卡片化
- 微信小程序真实产品感
- 克制、可信、不焦虑

禁止风格：

- 医院后台风
- 医疗问诊风
- 科技暗黑风
- 企业管理系统风
- 营销落地页风
- 资讯 App 风
- 电商 App 风
- 健身 App 风
- 社交 App 风
- 过度 AI 科幻风

---

## 视觉规范

### 颜色

主品牌色：

```css
#2EAB8A
```

页面背景：

```css
#F5F6F8
```

卡片背景：

```css
#FFFFFF
```

边框：

```css
#E8E8EE
```

主标题：

```css
#1A1D23
```

正文：

```css
#5A6270
```

辅助文字：

```css
#7B8493
```

浅品牌背景：

```css
#E8F6F1
```

状态辅助色：

```css
正常范围：#5B9E7A
需要关注：#E8913A
待观察：#8A9BB5
已记录：#7A8C9E
待完善：#B0A090
```

### 圆角

常用圆角：

```css
12rpx / 16rpx / 20rpx / 24rpx
```

卡片圆角：

```css
20rpx - 24rpx
```

按钮圆角：

```css
999rpx 或 16rpx
```

底部弹窗圆角：

```css
32rpx 32rpx 0 0
```

### 卡片

卡片样式要求：

- 白色背景
- 浅边框
- 极轻阴影
- 圆角
- 清晰层级
- 不要厚重投影
- 不要大面积纯色块
- 不要后台表格感

### 按钮

按钮要求：

- 清除微信默认 button 样式
- 不使用默认绿色按钮
- 不使用默认灰色按钮
- 主按钮使用品牌色
- 次按钮使用白底描边或浅品牌背景
- 按钮高度克制
- 圆角统一

---

## 文案规范

### 禁止出现的词

任何页面、mock 数据、注释中的展示文案，都不要出现以下词语：

- 异常
- 偏高
- 略高
- 诊断
- 治疗
- 用药
- 处方
- 高血压
- 糖尿病
- 痛风
- 复查
- 患者
- 病情
- 医生
- 问诊
- 病例
- 购买
- 下单
- 价格
- 支付

### 推荐使用的词

推荐使用以下安全、克制的表达：

- 需要关注
- 待观察
- 正常范围
- 已记录
- 空腹记录
- 建议继续记录
- 建议保持固定周期测量
- 记录较少
- 最近记录
- 趋势说明
- 记录解读
- 仅供参考
- 如有不适请咨询专业人士
- 当前查看
- 家庭成员
- 数据来源
- 关联记录
- 设备同步
- 关注指标

### AI 助手表达边界

AI 助手只能做：

- 记录解读
- 趋势说明
- 近期记录总结
- 继续记录建议
- 数据来源引导

AI 助手不能做：

- 诊断判断
- 治疗建议
- 药物建议
- 疾病结论
- 医疗问答

AI 回复中必须保持克制表达。

---

## 页面结构

### TabBar

底部主导航包含 3 个入口：

1. 健康（心电图图标）
2. AI 助手（机器人脸图标）
3. 我的（人形图标）

不要增加新的主 Tab。

---

## 页面列表（完整）

### 1. 健康首页 ✅

路径：
```text
pages/health/index
```

核心模块：
- 顶部导航区域（成员入口 + 提醒入口）
- 今日健康概览卡片（3 个统计格 + AI 入口）
- 2×2 指标卡片区（血压/血糖/尿酸/设备状态）
- 今日提醒

交互：
- 点击成员入口：打开成员快捷选择底部弹窗
- 点击提醒入口：进入提醒设置页
- 点击记录按钮：打开添加记录底部弹窗
- 点击趋势按钮：进入指标详情页
- 点击管理设备：进入设备管理页
- 点击全部记录：进入全部记录页
- 点击 AI 助手入口：Tab 切换到 AI 助手页

### 2. 添加记录底部弹窗 ✅

路径：
```text
components/add-record-sheet
```

在健康首页中触发。默认进入设备同步模式，有设备自动同步，无设备引导去商城购买，也可切换手动输入。

支持指标类型：
- 血压
- 血糖
- 尿酸

支持录入方式：
- 手动输入
- 设备同步

### 3. 指标详情页 ✅

路径：
```text
pages/detail/index
```

通过参数区分类型：
```text
/pages/detail/index?type=bloodPressure
/pages/detail/index?type=bloodGlucose
/pages/detail/index?type=uricAcid
```

模块：
- 顶部返回
- 核心测量值
- 状态标签
- 趋势图 CSS 占位
- 时间筛选
- 最近记录
- 查看全部记录入口

### 4. 全部记录页 ✅

路径：
```text
pages/records/index
```

功能：
- 指标类型筛选（全部/血压/血糖/尿酸）
- 时间筛选（近 7 天/近 30 天/全部）
- 按日期分组记录列表（今天/昨天/更早）
- 空状态
- 支持设备同步记录合并展示

### 5. 家庭成员页 ✅

路径：
```text
pages/family/index
```

功能：
- 展示家庭成员（本人/父亲/母亲）
- 当前查看标识
- 成员信息状态
- 添加成员入口占位

### 6. 家庭成员切换底部弹窗 ✅

路径：
```text
components/member-switch-sheet
```

功能：
- 3 个成员列表
- 当前查看成员品牌色标识
- 管理家庭成员入口

### 7. 设备管理页 ✅

路径：
```text
pages/device/index
```

功能：
- 已连接设备（含删除/同步/查看记录）
- 推荐添加设备（连接/商城双按钮）
- 设备连接底部弹窗（搜索→发现→连接）
- 设备同步生成 mock 记录

### 8. 提醒设置页 ✅

路径：
```text
pages/reminder/index
```

功能：
- 提醒列表（3 条）
- switch 开关
- 添加提醒入口占位

### 9. AI 助手页 ✅

路径：
```text
pages/ai/index
```

功能：
- 今日记录解读卡片
- 快捷问题（4 个）
- 对话区（AI 气泡 + 用户气泡）
- 输入区（底部固定）
- 打字机效果回复
- 历史会话抽屉

### 10. 我的页 ✅

路径：
```text
pages/my/index
```

功能：
- 用户信息卡片（头像/姓名/性别年龄/进度条）
- 健康档案四宫格（身高/体重/关注指标/记录习惯）
- 健康管理菜单（4 项）
- 通用设置菜单（2 项）
- 数据跟随当前成员切换

### 11. AI 会话抽屉 ✅

路径：
```text
components/ai-drawer
```

功能：
- 右侧滑出
- 历史会话列表（3 条）
- 新建会话

---

## 项目结构（完整）

```text
/
├── app.js                 # 全局数据：currentMember/members/syncedRecords 等
├── app.json               # 全局配置 + TabBar（3 Tab，PNG 图标）
├── app.wxss               # 全局样式 token + 通用组件样式
├── sitemap.json
├── project.config.json
├── CLAUDE.md
├── .gitignore
├── images/
│   ├── tab-*.png          # TabBar 图标（心电图/机器人脸/人形）
│   ├── tab-*.svg          # TabBar 图标源文件
│   └── icon-*.png         # 菜单图标（用户/提醒/设置/列表/锁/信息）
├── mock/
│   └── health.js          # Mock 数据（完整）
├── utils/
│   └── format.js          # 工具函数
├── components/
│   ├── add-record-sheet/  # 添加记录底部弹窗
│   │   └── index.*
│   ├── member-switch-sheet/ # 成员切换弹窗
│   │   └── index.*
│   └── ai-drawer/         # AI 会话抽屉
│       └── index.*
└── pages/
    ├── health/index.*     # 健康首页（Tab）
    ├── ai/index.*         # AI 助手（Tab）
    ├── my/index.*         # 我的（Tab）
    ├── detail/index.*     # 指标详情页
    ├── records/index.*    # 全部记录页
    ├── family/index.*     # 家庭成员页
    ├── device/index.*     # 设备管理页
    └── reminder/index.*   # 提醒设置页
```

---

## Mock 数据说明

文件路径：
```text
mock/health.js
```

包含以下导出字段：

| 字段 | 说明 |
|---|---|
| members | 3 个成员（本人/母亲/父亲），含 height/weight/focusIndicators/recordHabit/completion |
| metricTypes | 3 种指标配置 |
| healthRecords | 3 条基础记录 |
| detailRecords | 12 条详情记录（每类型 4 条） |
| deviceList | 已连接设备列表 |
| recommendedDevices | 推荐设备列表 |
| reminders | 3 条提醒设置 |
| todayReminders | 3 条今日提醒 |
| todayOverview | 今日概览 |
| homeIndicators | 首页指标卡片数据（默认本人） |
| memberHomeData | 3 成员独立首页数据 |
| typeConfig | 详情页 3 类型配置 |
| aiInsight | 今日记录解读 |
| initialAiMessage | AI 初始消息 |
| quickQuestions | 4 个快捷问题 |
| aiReplyMap | 5 条关键词回复映射 |
| fallbackReply | 兜底回复 |
| aiSessions | 3 条历史会话 |

全局运行时数据（app.globalData）：

| 字段 | 说明 |
|---|---|
| currentMember | 当前查看成员 |
| members | 家庭成员列表 |
| deviceList | 运行时连接的设备 |
| syncedRecords | 设备同步生成的记录 |
| removedDeviceIds | 用户移除的设备 ID |

---

## 开发阶段规划（完成状态）

### Phase 1 ✅ 项目初始化
创建小程序基础结构、app.json/tabBar、全局样式、mock 数据骨架、3 个 Tab 页面骨架、5 个空页面目录。

### Phase 2 ✅ 首页高保真还原
健康首页：顶部操作行、今日概览卡片、2×2 指标卡片、今日提醒模块、基础跳转入口。

### Phase 3 ✅ 添加记录闭环
添加记录弹窗组件：类型切换、表单填写、保存→首页更新。遮罩层级正确（兄弟节点）。

### Phase 3.1 ✅ 弹窗优化
弹窗时隐藏 tabBar、输入框单位对齐加固。

### Phase 4 ✅ 指标详情 + 全部记录
详情页 3 类型复用、CSS 趋势图占位、最近记录、全部记录页筛选/日期分组。

### Phase 4.1 ✅ 详情页修正
血糖/尿酸核心数值空白修复、趋势图阈值标注移除。

### Phase 5 ✅ 家庭成员切换
成员切换弹窗组件、切换成员后首页数据跟随、家庭成员页、全局状态同步。

### Phase 6 ✅ 设备管理 + 提醒设置
设备管理页（已连接/推荐/同步）、提醒设置页（列表/switch）。

### Phase 7 ✅ AI 助手页
今日洞察卡片、快捷问题、对话区、打字机效果、历史会话抽屉、安全表达边界。

### Phase 7.1 ✅ AI 页优化
发送图标居中、消息自动滚动。

### Phase 7.2 ✅ AI 等待状态
思考中占位动画、延迟后逐字输出。

### Phase 7.5 ✅ 我的页
用户卡片、健康档案、功能菜单、数据跟随成员切换、Lucide 图标。

### Phase 8 ✅ 最终收口
全项目风险文案扫描（0 违规）、mock 数据一致性检查、视觉一致性检查。

### Phase 8.5 ✅ 设备同步记录闭环
设备同步生成 mock 记录、首页/详情页/记录页合并展示、设备连接流程（搜索→发现→连接）、设备删除/重新添加。

---

## 开发流程规则

每次开始新阶段前，必须先输出：

```text
本阶段目标：
本阶段将修改：
本阶段不做：
风险点：
```

得到确认后再写代码。

每次完成后，必须输出：

```text
已修改文件：
完成内容：
仍是占位的内容：
如何预览：
下一阶段建议：
```

不要一次性跨阶段开发。

---

## 当前仍为占位的内容

| 内容 | 说明 |
|---|---|
| 设备同步记录详情 | toast "后续接入" |
| 添加家庭成员 | toast "后续接入" |
| 添加提醒 | toast "后续接入" |
| 隐私与数据 | toast "后续接入" |
| 关于瓯械健康 | toast "瓯械健康 Demo" |
| 趋势图 | CSS 占位折线，非真实数据图表 |
| 时间筛选 | 仅 UI 切换，未做真实数据过滤 |
| AI 真实 API | 所有回复为本地关键词映射 |
| 设备商城跳转 | toast "即将跳转至商城" |

---

## 代码风格

### WXML

要求：

- 结构清晰
- class 命名语义化
- 不写复杂内联样式
- 不写大量重复结构
- 列表使用 wx:for
- 条件显示使用 wx:if / hidden

### WXSS

要求：

- 优先使用 app.wxss 通用样式
- 页面样式只写页面特有内容
- 不要重复定义大量颜色
- 统一圆角、阴影、边框
- 清除 button 默认样式
- 不要使用过度复杂选择器

### JavaScript

要求：

- 数据从 mock 文件读取
- 页面状态清晰
- 方法命名清楚
- 不写无用代码
- 不写真实接口请求
- 不写复杂异步逻辑
- 保存记录时只更新本地页面状态

### 注释

允许少量注释说明关键交互。

不要写大量无意义注释。

---

## 底部弹窗规范

所有底部弹窗必须遵守：

- mask 与 sheet 分离
- mask z-index 低于 sheet
- sheet 不透明
- 点击 mask 关闭
- 点击 sheet 不关闭
- 页面滚动不要穿透
- 弹窗不遮挡底部安全区
- 适配 iPhone 底部安全距离

### 遮罩层级要求（必须遵守）

正确结构：
```xml
<view wx:if="{{showSheet}}" class="sheet-mask" bindtap="closeSheet"></view>
<view wx:if="{{showSheet}}" class="sheet-body" catchtap="noop">
  <!-- 弹窗内容 -->
</view>
```

禁止结构：
```xml
<view class="sheet-mask">
  <view class="sheet-body"><!-- 错误：弹窗被放进遮罩里 --></view>
</view>
```

### 弹窗互斥

添加记录弹窗和成员切换弹窗不能同时打开。
打开一个弹窗前如果另一个弹窗处于打开状态，先关闭。

---

## 图片资源说明

TabBar 图标位于 `images/` 目录：
- `tab-health.png` — 心电图（健康）
- `tab-ai.png` — 机器人脸（AI 助手）
- `tab-my.png` — 人形（我的）

菜单图标位于 `images/` 目录：
- `icon-user.png` — 家庭成员
- `icon-bell.png` — 提醒设置
- `icon-settings.png` — 设备管理
- `icon-list.png` — 全部记录
- `icon-lock.png` — 隐私与数据
- `icon-info.png` — 关于

所有图标均来自 Lucide 风格，SVG 源文件同时保留在 `images/` 目录中。

---

## 一句话项目原则

这是一个用于作品集展示的轻医疗家庭健康管理微信小程序前端 Demo。请优先保证视觉还原、交互闭环和安全克制表达，不要把它做成后台系统、问答工具、医疗产品或通用组件库项目。
