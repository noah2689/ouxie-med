# CLAUDE.md

## 项目名称

瓯械健康

## 项目类型

家庭健康管理微信小程序前端 Demo

## 项目目标

本项目用于 UI/UX 作品集展示，目标是开发一个可交互的微信小程序前端 Demo，用于展示家庭健康指标记录、家庭成员切换、指标详情查看、设备管理、提醒设置和 AI 辅助解读等核心流程。

当前阶段只做前端 Demo：

- 不接真实后端
- 不做登录系统
- 不接真实设备
- 不调用真实 AI
- 不做真实数据同步
- 所有数据使用本地 mock 数据
- 重点是还原高保真设计和交互流程

项目不是医疗问诊工具，不提供诊断、治疗、处方或用药建议。

---

## 技术栈

使用微信小程序原生开发：

- WXML
- WXSS
- JavaScript
- JSON
- 本地 mock 数据

禁止使用：

- uni-app
- Taro
- React
- Vue
- 第三方 UI 组件库
- TDesign 整套接入
- 暗黑模式
- 外部图表库
- 后端接口
- 云开发
- 登录授权

如确实需要 Toast、Picker、Popup 等基础交互，可以参考组件库的交互思路，但不得引入整套 UI 框架，也不得破坏当前高保真视觉风格。

---

## 核心开发原则

### 1. 高保真优先

本项目首先是作品集 Demo，不是业务上线项目。

开发优先级：

1. 视觉还原
2. 核心交互闭环
3. 页面完整度
4. 数据结构清晰
5. 代码可维护

不要为了快速完成功能而牺牲视觉还原。

### 2. 分阶段开发

不要一次性开发所有页面。

每次只做一个阶段，完成后停止，等待人工验收。

开发时必须先说明：

- 本阶段目标
- 将修改哪些文件
- 为什么修改这些文件
- 本阶段不做什么

完成后必须说明：

- 修改了哪些文件
- 完成了哪些功能
- 哪些只是 mock 或占位
- 下一阶段建议做什么

### 3. 不允许自行扩展需求

不得自行新增以下内容：

- 登录注册
- 用户权限
- 后端 API
- 真实设备绑定
- 真实 AI 对话
- 会员体系
- 商城
- 资讯流
- 健康报告下载
- 复杂图表库
- 暗黑模式
- 医疗问答功能

如果发现需要扩展，先停止并询问。

---

## 产品定位

瓯械健康是一个面向家庭场景的健康指标记录与管理小程序。

核心能力：

- 查看家庭成员健康概览
- 记录血压、血糖、尿酸
- 查看指标详情和历史记录
- 切换家庭成员
- 管理设备状态
- 设置记录提醒
- 使用 AI 助手辅助理解近期记录变化

产品关键词：

- 家庭健康管理
- 指标记录
- 多成员数据
- 趋势查看
- 设备管理
- 提醒设置
- AI 辅助解读
- 轻医疗
- 克制表达
- 数据记录工具

---

## 设计风格

整体风格：

- 清爽
- 专业
- 轻医疗
- 家庭健康管理
- 数据卡片化
- 微信小程序真实产品感
- 克制、可信、不焦虑

禁止风格：

- 医院后台风
- 医疗问诊风
- 科技暗黑风
- 企业管理系统风
- 营销落地页风
- 资讯 App 风
- 电商 App 风
- 健身 App 风
- 社交 App 风
- 过度 AI 科幻风

---

## 视觉规范

### 颜色

主品牌色：

```css
#2EAB8A
```

页面背景：

```css
#F5F6F8
```

卡片背景：

```css
#FFFFFF
```

边框：

```css
#E8E8EE
```

主标题：

```css
#1A1D23
```

正文：

```css
#5A6270
```

辅助文字：

```css
#7B8493
```

浅品牌背景：

```css
#E8F6F1
```

状态辅助色：

```css
正常范围：#5B9E7A
需要关注：#E8913A
待观察：#8A9BB5
已记录：#7A8C9E
待完善：#B0A090
```

### 圆角

常用圆角：

```css
12rpx / 16rpx / 20rpx / 24rpx
```

卡片圆角：

```css
20rpx - 24rpx
```

按钮圆角：

```css
999rpx 或 16rpx
```

底部弹窗圆角：

```css
32rpx 32rpx 0 0
```

### 卡片

卡片样式要求：

- 白色背景
- 浅边框
- 极轻阴影
- 圆角
- 清晰层级
- 不要厚重投影
- 不要大面积纯色块
- 不要后台表格感

### 按钮

按钮要求：

- 清除微信默认 button 样式
- 不使用默认绿色按钮
- 不使用默认灰色按钮
- 主按钮使用品牌色
- 次按钮使用白底描边或浅品牌背景
- 按钮高度克制
- 圆角统一

---

## 文案规范

### 禁止出现的词

任何页面、mock 数据、注释中的展示文案，都不要出现以下词语：

- 异常
- 偏高
- 略高
- 诊断
- 治疗
- 用药
- 处方
- 高血压
- 糖尿病
- 痛风
- 复查
- 患者
- 病情
- 医生
- 问诊
- 病例

### 推荐使用的词

推荐使用以下安全、克制的表达：

- 需要关注
- 待观察
- 正常范围
- 已记录
- 空腹记录
- 建议继续记录
- 建议保持固定周期测量
- 记录较少
- 最近记录
- 趋势说明
- 记录解读
- 仅供参考
- 如有不适请咨询专业人士
- 当前查看
- 家庭成员
- 数据来源
- 关联记录

### AI 助手表达边界

AI 助手只能做：

- 记录解读
- 趋势说明
- 近期记录总结
- 继续记录建议
- 数据来源引导

AI 助手不能做：

- 诊断判断
- 治疗建议
- 药物建议
- 疾病结论
- 医疗问答

AI 回复中必须保持克制表达。

---

## 页面结构

### TabBar

底部主导航包含 3 个入口：

1. 健康
2. AI 助手
3. 我的

不要增加新的主 Tab。

---

## 页面列表

### 1. 健康首页

路径：

```text
pages/health/index
```

核心模块：

- 顶部导航区域
- 当前成员入口
- 提醒入口
- 今日健康概览
- AI 助手入口
- 2×2 指标卡片区
  - 血压
  - 血糖
  - 尿酸
  - 设备状态

- 今日提醒

交互：

- 点击成员入口：打开成员快捷选择底部弹窗
- 点击提醒入口：进入提醒设置页
- 点击记录按钮：打开添加记录底部弹窗
- 点击趋势按钮：进入指标详情页
- 点击管理设备：进入设备管理页
- 点击全部记录：进入全部记录页
- 点击 AI 助手入口：进入 AI 助手页

### 2. 添加记录底部弹窗

在健康首页中触发。

支持指标类型：

- 血压
- 血糖
- 尿酸

支持录入方式：

- 手动输入
- 设备同步

弹窗要求：

- 页面背景变暗
- 弹窗本体保持白色、不透明
- 遮罩层和弹窗必须是兄弟节点
- 遮罩 z-index 低于弹窗
- 点击遮罩关闭
- 点击弹窗内容不关闭
- 保存后更新 mock 数据并关闭弹窗
- 保存后首页数据同步变化

正确结构示例：

```xml
<view wx:if="{{showRecordSheet}}" class="sheet-mask" bindtap="closeRecordSheet"></view>

<view wx:if="{{showRecordSheet}}" class="record-sheet" catchtap="noop">
  <!-- 弹窗内容 -->
</view>
```

禁止结构：

```xml
<view class="sheet-mask">
  <view class="record-sheet">
    <!-- 错误：弹窗被放进遮罩里 -->
  </view>
</view>
```

### 3. 指标详情页

路径：

```text
pages/detail/index
```

通过参数区分类型：

```text
/pages/detail/index?type=bloodPressure
/pages/detail/index?type=bloodGlucose
/pages/detail/index?type=uricAcid
```

详情页复用同一模板，通过 type 参数切换内容。

模块：

- 顶部返回
- 指标名称
- 当前成员
- 核心测量值
- 状态标签
- 趋势图占位
- 时间筛选
- 最近记录
- 查看全部记录入口

点击查看全部记录时，进入全部记录页，并保留当前指标筛选。

### 4. 全部记录页

路径：

```text
pages/records/index
```

功能：

- 指标类型筛选
- 成员筛选
- 时间筛选
- 历史记录列表
- 空状态

允许通过参数进入：

```text
/pages/records/index?type=bloodPressure
```

### 5. 家庭成员页

路径：

```text
pages/family/index
```

功能：

- 展示家庭成员
- 本人 / 父亲 / 母亲
- 当前查看标识
- 成员信息状态
- 添加成员入口占位

### 6. 设备管理页

路径：

```text
pages/device/index
```

功能：

- 已连接设备
- 设备同步状态
- 添加设备入口占位
- 支持血压计、血糖仪、尿酸仪等 mock 数据

不做真实蓝牙，不做真实设备绑定。

### 7. 提醒设置页

路径：

```text
pages/reminder/index
```

功能：

- 未记录提醒
- 固定周期记录提醒
- 提醒开关
- 提醒时间
- 提醒对象

只做本地交互，不做真实通知权限。

### 8. AI 助手页

路径：

```text
pages/ai/index
```

功能：

- 常用问题
- 近期记录入口
- 对话输入区
- mock AI 回复
- 关联记录入口

AI 回复必须强调：

- 记录解读
- 趋势说明
- 仅供参考
- 可查看关联记录

---

## Mock 数据要求

建议建立：

```text
mock/health.js
```

包含：

- 当前成员
- 家庭成员列表
- 指标数据
- 今日概览
- 今日提醒
- 最近记录
- 设备列表
- 提醒设置
- AI 示例问题
- AI 示例回复

数据结构要清晰，字段命名统一。

建议指标类型：

```js
bloodPressure;
bloodGlucose;
uricAcid;
deviceStatus;
```

成员示例：

```js
self;
father;
mother;
```

不要在页面中硬编码大量数据，优先从 mock 文件读取。

---

## 推荐项目结构

```text
/
├── app.js
├── app.json
├── app.wxss
├── sitemap.json
├── CLAUDE.md
├── mock/
│   └── health.js
├── utils/
│   └── format.js
├── pages/
│   ├── health/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── detail/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── records/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── family/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── device/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── reminder/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   ├── ai/
│   │   ├── index.wxml
│   │   ├── index.wxss
│   │   ├── index.js
│   │   └── index.json
│   └── my/
│       ├── index.wxml
│       ├── index.wxss
│       ├── index.js
│       └── index.json
```

---

## 开发阶段规划

### Phase 1：项目初始化

目标：

- 创建微信小程序基础结构
- 配置 app.json
- 配置 tabBar
- 创建 app.wxss 全局样式
- 创建 mock 数据
- 创建 3 个主 Tab 页面骨架

只做：

- 健康
- AI 助手
- 我的

不做复杂功能。

### Phase 2：首页高保真还原

目标：

- 完成健康首页视觉还原
- 对齐高保真首页结构
- 完成 2×2 指标卡片
- 完成今日概览
- 完成今日提醒
- 完成基础跳转入口

不做添加记录弹窗。

### Phase 3：添加记录闭环

目标：

完成第一个核心交互闭环：

```text
点击记录 → 打开底部弹窗 → 填写数据 → 保存 → 首页更新
```

重点：

- 遮罩层级正确
- 弹窗白色不透明
- 表单视觉接近高保真
- 保存后 mock 数据更新
- 首页今日概览同步变化

### Phase 4：指标详情与全部记录

目标：

- 完成指标详情页
- 完成 type 参数切换
- 完成趋势图占位
- 完成最近记录
- 完成查看全部记录
- 完成全部记录页筛选

### Phase 5：家庭成员切换

目标：

- 完成成员快捷选择底部弹窗
- 切换当前成员
- 首页数据跟随成员更新
- 完成家庭成员页

### Phase 6：设备与提醒

目标：

- 完成设备管理页
- 完成提醒设置页
- 完成基础本地交互

### Phase 7：AI 助手

目标：

- 完成 AI 助手页
- 完成 mock 提问
- 完成 mock 回复
- 完成关联记录入口
- 保持安全表达边界

### Phase 8：作品集收口

目标：

- 全流程可演示
- 页面风格统一
- mock 数据自然
- 没有高风险医疗文案
- 没有明显 bug
- 可以录屏或截图放入作品集

---

## 开发流程规则

每次开始新阶段前，必须先输出：

```text
本阶段目标：
本阶段将修改：
本阶段不做：
风险点：
```

得到确认后再写代码。

每次完成后，必须输出：

```text
已修改文件：
完成内容：
仍是占位的内容：
如何预览：
下一阶段建议：
```

不要一次性跨阶段开发。

---

## 代码风格

### WXML

要求：

- 结构清晰
- class 命名语义化
- 不写复杂内联样式
- 不写大量重复结构
- 列表使用 wx:for
- 条件显示使用 wx:if / hidden

### WXSS

要求：

- 优先使用 app.wxss 通用样式
- 页面样式只写页面特有内容
- 不要重复定义大量颜色
- 统一圆角、阴影、边框
- 清除 button 默认样式
- 不要使用过度复杂选择器

### JavaScript

要求：

- 数据从 mock 文件读取
- 页面状态清晰
- 方法命名清楚
- 不写无用代码
- 不写真实接口请求
- 不写复杂异步逻辑
- 保存记录时只更新本地页面状态

### 注释

允许少量注释说明关键交互。

不要写大量无意义注释。

---

## 交互注意事项

### 底部弹窗

所有底部弹窗必须遵守：

- mask 与 sheet 分离
- mask z-index 低于 sheet
- sheet 不透明
- 点击 mask 关闭
- 点击 sheet 不关闭
- 页面滚动不要穿透
- 弹窗不遮挡底部安全区
- 适配 iPhone 底部安全距离

### 跳转

使用：

```js
wx.navigateTo();
```

Tab 页面使用：

```js
wx.switchTab();
```

不要错误使用跳转方式。

### Toast

可以使用：

```js
wx.showToast();
```

但文案要克制。

---

## 验收标准

每个阶段的验收标准：

- 页面能在微信开发者工具运行
- 无明显控制台报错
- 无明显样式错乱
- 无明显遮挡问题
- 不出现高风险医疗词
- 视觉接近高保真
- 交互路径可演示
- mock 数据合理
- 代码结构清晰

---

## 当前重点提醒

当前项目要重新开发时，请不要继承之前写坏的结构。

可以参考之前的设计资料，但代码实现应重新按阶段搭建。

优先顺序：

1. 先把首页做准
2. 再做添加记录弹窗
3. 再做详情页和记录页
4. 最后补家庭、设备、提醒、AI

不要一开始就开发全部页面。

---

## 给 Claude 的行为要求

你是本项目的前端开发助手。

你必须：

- 严格按阶段开发
- 严格遵守视觉规范
- 严格遵守文案规范
- 不擅自扩展需求
- 不擅自引入库
- 不擅自做后端
- 不擅自做暗黑模式
- 不擅自改变产品定位
- 每次修改前说明计划
- 每次修改后说明结果

如果需求不明确，先提出最小必要问题。

如果发现当前实现和高保真冲突，优先遵守高保真设计。

如果功能完整度和视觉还原冲突，优先视觉还原。

---

## 一句话项目原则

这是一个用于作品集展示的轻医疗家庭健康管理微信小程序前端 Demo。请优先保证视觉还原、交互闭环和安全克制表达，不要把它做成后台系统、问答工具、医疗产品或通用组件库项目。
